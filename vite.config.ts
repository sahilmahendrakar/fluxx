/**
 * Root Vite config so the shadcn CLI can detect this Electron renderer setup.
 * Electron Forge uses `vite.renderer.config.ts` for the actual renderer build.
 */
export { default } from './vite.renderer.config';

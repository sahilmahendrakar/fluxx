/** Built-in local device id in the global per-machine registry. */
export const BUILTIN_LOCAL_DEVICE_ID = 'local';

export const BUILTIN_LOCAL_DEVICE_DISPLAY_NAME = 'This computer';

export const DEFAULT_LOCAL_WORKSPACE_ROOT = '~/.fluxx/worktrees';

export const EXECUTION_DEVICES_FILE_SCHEMA_VERSION = 1;

/** Flux-managed region inside `planning/CLAUDE.md` and `planning/AGENTS.md` (HTML comments — inert in most Markdown renderers). */
export const FLUX_PLANNING_INSTRUCTIONS_BEGIN = '<!-- FLUX_PLANNING_INSTRUCTIONS:BEGIN -->';
export const FLUX_PLANNING_INSTRUCTIONS_END = '<!-- FLUX_PLANNING_INSTRUCTIONS:END -->';

/** Local-only metadata for Flux-managed instruction upgrades (not a planning doc). */
export const PLANNING_INSTRUCTIONS_STATE_BASENAME = '.flux-instructions.json';
